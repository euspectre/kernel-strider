[group]
	# Name of the target function
	function.name = __krealloc
	
	# The handlers are defined in the template because they are
	# function-specific.
#######################################################################
